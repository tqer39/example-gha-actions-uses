---
name: "Composite Action"
description: "[Test] Composite Action"

runs:
  using: "composite"
  steps:
    - name: "Checkout"
      uses: actions/checkout@v3

    - name: "Hello World"
      shell: bash
      run: echo 'Hello World'